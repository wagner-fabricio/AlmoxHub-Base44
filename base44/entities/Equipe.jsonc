{
  "name": "Equipe",
  "type": "object",
  "properties": {
    "nome": {
      "type": "string",
      "description": "Nome da equipe"
    },
    "descricao": {
      "type": "string",
      "description": "Descri\u00e7\u00e3o da equipe"
    },
    "lider_id": {
      "type": "string",
      "description": "ID do l\u00edder da equipe"
    },
    "membros_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "IDs dos membros da equipe"
    },
    "regional_id": {
      "type": "string",
      "description": "ID da regional vinculada"
    },
    "almoxarifados_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "IDs dos almoxarifados que a equipe atende"
    },
    "cor": {
      "type": "string",
      "description": "Cor identificadora da equipe (hex)"
    },
    "ativa": {
      "type": "boolean",
      "default": true,
      "description": "Se a equipe est\u00e1 ativa"
    }
  },
  "required": [
    "nome",
    "lider_id"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "data.lider_id": "{{user.id}}"
        },
        {
          "data.membros_ids": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.lider_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.lider_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}